pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Checkout the code from the Git repository
                git 'https://github.com/abhattiab/Jenkins_Pipe_Line.git'

                // Your build commands go here
                echo 'Building...'
            }
        }
    }
}
